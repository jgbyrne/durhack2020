input InputItem {

    name: String!
    url: String!
    type: ItemType!

}

type Item {
    _id: ID!

    name: String!
    url: String!
    type: ItemType!
    thumbnail: String! #computed at run time

    flowItems: [FlowItem!]!
    flowItem(flow: ID): FlowItem

}


enum ItemType {
    Album
    #    Song
    #    YouTube
}
